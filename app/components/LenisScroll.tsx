'use client';

// Lenis smooth scrolling disabled - using native browser scrolling for better performance
// No auto-scroll, no laggy effects - just native browser behavior

export default function LenisScroll({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
